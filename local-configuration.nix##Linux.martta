{ users, pkgs }:
{
  hostName = "martta";
  grubDevice = "/dev/sda";
  version = "17.03";
  users = with users; [
    (jluttine { groups = [ "wheel" ]; })
    (meri { groups = [ "wheel" ]; })
  ];
  displayManager = "sddm";
  desktopEnvironment = "kde";
  allowUnfree = true;
  extraPackages = with pkgs; [

    # Instant messaging
    #tdesktop
    #qtox
    #linphone

    dropbox

  ];
}
