{ users, pkgs }:
{
  hostName = "martta";
  grubDevice = "/dev/sda";
  users = with users; [
    (jluttine { groups = [ "wheel" ]; })
    (meri { groups = [ "wheel" ]; })
  ];
  displayManager = "sddm";
  desktopEnvironment = "kde";
  extraServices = {
    syncthing = {
      enable = true;
      user = "meri";
    };
  };
  allowUnfree = true;
  extraPackages = with pkgs; [

    # Instant messaging
    #tdesktop
    #qtox
    #linphone

    dropbox

  ];
}
